package common

import (
	/*"os"
	"os/exec"
	"path/filepath"
	"strings"*/
)

func GetBasePath() string {
	/*file, _ := exec.LookPath(os.Args[0])
	path, _ := filepath.Abs(file)
	index := strings.LastIndex(path, "\\")
	return path[0 : index+1]*/
	return "E:\\cloud-sky\\bin"
}